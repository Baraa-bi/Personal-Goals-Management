import { CheckDateDirective } from './check-date.directive';

describe('CheckDateDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckDateDirective();
    expect(directive).toBeTruthy();
  });
});
