import { GoalProgressDirective } from './goal-progress.directive';

describe('GoalProgressDirective', () => {
  it('should create an instance', () => {
    const directive = new GoalProgressDirective();
    expect(directive).toBeTruthy();
  });
});
