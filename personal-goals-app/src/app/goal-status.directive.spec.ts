import { GoalStatusDirective } from './goal-status.directive';

describe('GoalStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new GoalStatusDirective();
    expect(directive).toBeTruthy();
  });
});
